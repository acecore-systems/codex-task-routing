# 検証記録

## 0.8.2: 診断・テスト・リリース検査の改善

2026-09-23、Windows / Python 3.13の通常サンドボックス内で134件のテストと `python scripts/check_package.py` が成功しました。隔離Codexホームではnative CLIによる導入・両フック起動・再インストール・解除と個別設定の保持も確認しました。稼働中アプリへの再読み込み、通常Chat画面、子モデルの実行、利用量は今回の検証に含みません。CIには既存の3 OS / Python 3.11に加えてWindows / Python 3.13を追加し、リモート結果はPRで確認します。

- 変更前は一時フォルダへのアクセス拒否が発生しました。テスト用フォルダをcheckout配下でACLを継承して作成する方式に統一した対象3ファイルは、権限変更やサンドボックス外実行なしで成功しました。
- Windowsのrename再試行テストは、疑似エラーで試行枠を消費した後に実ファイル操作を混ぜていたため、全体実行では失敗、単独では成功しました。再試行判断の単体テストと実ファイルへの公開・読戻しの結合テストを分離しています。実装側の再試行回数や待機時間は変更していません。
- `status --json` は固定の `error_code` / `hint` を返し、未知キー、JSON、effort範囲、テンプレート、ファイル異常を区別します。入力した秘密値、未知キー名、OSエラー本文の非表示と設定ファイルの無変更を回帰テストで確認しています。追加レビューで、不正UTF-8やサイズ超過のテンプレートも `invalid_template` として案内するよう修正し、回帰テストを追加しました。
- 観測記録を `scripts/routing_core/observation.py`、共有I/Oを `common.py` へ移動しました。観測26関数のASTが変更前と同一であること、既存CLI、観測台帳、ロック、配布フックのrunpy起動を確認しています。分担方針・既定設定・記録形式は変更していません。
- リリース検査はSemVerの増加を要求し、後退・同一版・build metadataのみの変更を拒否します。数値比較、プレリリース、不正入力、非配布変更のテストを追加しました。
- READMEを導入・確認・任意機能・トラブル対応中心に整理し、過去版の説明を変更履歴へ移しました。

## 0.8.1: PR #11の追加レビュー

2026-09-15、固定HEAD `6f57344a35813698b30ae4010cf350a1901dcc0e` と基準版の25ファイル差分、方針・接続判定・引継ぎ互換・テストをレビューしました。

- 元の116件のテストスイートは成功（環境依存2件skip）。入力は256KiB以内でも、JSONの再エスケープでprepare出力が約483KBとなり、後続のvalidate/転送で読めなくなるケースを再現しました。CLIの実出力サイズを準備時に検査し、超過時に未使用のbundleを出力しないようにしました。
- 初回の書込み等について、未実行の`permission_checked`を追加。操作スキーマ・対象権限の根拠と新しいlive確認を要求し、試験書込みや実行済みの誤申告なしで本来の依頼を候補にできます。advertisedの単独利用、別操作/対象への流用、期限切れ、承認不足は引き続き拒否します。
- 追加8件の回帰テストで、大きいUnicode/引用符、サイズ境界の受渡し、エラー時の出力・入力保護、初回操作の各ゲートを検証します。実行結果と最終3 OS CIはPRレビューに記録します。
- 実際のCodex導入・子モデル起動・Chat画面・接続先での実操作・利用量は未検証で、利用者がCodex側で後続確認します。通常Chat自動送受信の許可を新たに確認したものでも、経路を有効化したものでもありません。

## 0.8.0: Terra親・固定effort・能力に応じたChat優先

2026-09-15、Issue #10の方針を実装しました。意味を変えたのは、通常工程を親で完結、Terra子を標準外、自動effortを有効なdefault_effortへ固定、6 Proを適合工程の最初の主担当候補、並列とホスト許可付き直列を区別する点です。Chatで利用できる機能・権限・検証環境がある実装・PR作成も対象にしました。

- 既存のmin/maxと部分overrideを保持し、親の保存設定・実行設定・フック信頼・Chat opt-inは変更しません。新しい自動有効化やモデル呼出しはありません。
- chat_planの旧入力は並列のまま。直列は明示した追加価値とホスト可否を要求します。--liveは直近のsurface/model/上限通知/承認/転送方法の観測を照合しますが、実アカウントや許可の証明ではありません。
- 旧0.4/0.5/0.6/0.7の準備済みbundleを厳密照合する互換処理を保持します。新規promptは実装・PRの明示範囲、環境と検証結果の返却を要求し、merge/deployの暗黙承認を禁止します。
- ローカルLinux/Python 3.13.5で116件成功（環境依存2件skip）、package・diff検査成功。新規12件は固定値とoverride、Terra子の標準除外、旧bundle、直列の可否、liveの各ゲート、期限、read/writeとsurfaceの分離、CLIの入力保護を確認しました。
- UI、6 Proの実利用上限、子の実行モデル、実プラグイン導入、タスク単位の節約効果は未検証です。自動抽出方法の例外許可は未確認のままで、許可のない自動経路を有効化していません。最終3 OS CIはPRの結果を参照してください。
- [比較計画と利用条件](../plugins/codex-task-routing/skills/task-routing/references/chat-limits.md)に従い、通常作業の測定範囲がそろうまで節約率を主張しません。

## 0.7.1: PR #9の境界値レビュー

2026-09-15、PR #9のローカル転送、接続台帳、キャッシュ公開の差分を確認しました。

- JSONで表現できてもUTF-8へ保存できない単独サロゲートを、共通の文字列検証で拒否します。依頼のhash生成前・回答の保存前に安全なプロトコルエラーを返し、修正した回答は同じ転送で受理できます。通常の補助平面Unicodeは保持します。
- 接続期限の上限判定を日時への加算から差分比較へ変更し、UTC変換の範囲外も安全な入力エラーとして扱います。
- CIから取得したPRソースをLinux/Python 3.13.5で検証し、104件の単体テストが成功（環境依存2件はskip）、package検査とdiffの空白検査も通過しました。初回の旧101件では既存の子プロセス起動テストが1件失敗しましたが、対象再実行と修正後の全件で成功しました。タイミング依存の可能性は残します。
- Browser実画面、Windowsの実プラグイン導入、実行モデル・利用量の測定は今回追加していません。3 OSの最終コミットの結果はPRのCIを参照してください。

## 0.6.0: Chat優先判定と引継ぎ契約

有効なrootの工程選定にChat適合の優先判定を加え、分類表・スキル・起動時の追加指示をそろえました。既定オフ、子からのChat再委譲禁止、親の独立作業、nativeモデルとeffortの範囲は維持しています。

任意のhandoffは必要情報・出典・鮮度・許可ツール・返却形式・停止条件の6項目を厳密に検証し、入力hashへ含めます。旧形式の依頼と0.4・0.5系の処理中bundleの照合を維持し、handoff改変、部分指定、不正型、過大な入力、改変した旧promptは拒否します。設定無効時にChat優先指示を配信しないことと子への非配信も検証します。

Windows/Python 3.13で全80件の単体テスト、パッケージ検査、skill-creatorのスキル検査、文書のJSON例のprepare、git diff --checkを通過しました。

変更前の0.5.0では、承認済みの方針レビューを一時Chatの6 Proへ1回送り、回答の回収、依頼IDとhashの照合、送信前後のUIモデル確認まで実施しました。0.6.0の新しいpromptとMCP能力確認の実機実行、実バックエンドID、節約量は未検証です。既存の実機確認を新しいpromptの成功と扱いません。

## 0.5.0: 通常Chat一時Chatの優先経路

`browser-temporary` を新規設定の既定transportにし、既存の明示的な `codex-app-tools` はlegacyとして設定を書き換えずに受理します。通常Chatの一時Chatでは、サポートされたBrowser操作で本文を一度だけ送り、最終回答を取得します。複数行の本文は貼り付けまたは対応したfillで入力欄の全文照合後に送信し、`send_message_to_thread` は使いません。更新前の0.4系で生成済みの依頼bundleは、旧形式そのものと依頼ID・材料hashを照合できる場合だけ回収できます。

2026-09-14の実機確認では、最初に複数行本文を `typeText` で送ると改行ごとに断片送信されたため失敗と判定し、保存せず一時Chatを閉じました。`paste({format:'text'})` に替え、アクセシビリティツリーで入力欄全文を照合して送信ボタンを1回だけ使った後は、通常Chat・一時Chat・`6 Pro` 表示を送信前後で確認し、約30秒で期待した回答と依頼ID・材料hashの一致を確認しました。結果をローカルに保存後、保存操作をせずにタブを閉じ、新規ホームの最近の会話一覧が実行前後で同じことを確認しています。これはこの1件のUI経路の確認であり、バックエンドのモデルID、別環境での再現、ブラウザー不要の正規外部入口は未確認です。公開上の課題は `docs/backlog.md` に残します。

同日にPython 3.13で全77件の単体テストと `python scripts/check_package.py` を通過しました。隔離Codexホームでnative CLIによる導入・再インストール・解除と設定保持も確認し、manifestとskillの検証を通過しました。リモートの子プロセス停止テスト修正も取り込み済みです。自動テストはBrowser UIの実行を代替しません。

## 0.4.0: 観測機能と既定オフの通常Chat経路の統合

2026-09-14、Windows / Python 3.13で全74テスト、package、manifest、skill検証を通過しました。0.3.1の観測機能と0.3.0試験版の通常Chat経路を統合し、通常Chatは引き続き既定オフです。

- 実際のCodex CLIを使う一時ホームで、設定なしの初回導入ではChat経路が追加されず、観測機能が利用できることを確認しました。
- 同じ一時ホームで個別設定をオンにし、再導入・解除でそのファイルと既存の方針上書きが保持されることを確認しました。
- オンからオフへ戻した後も標準方針と予約済みの観測記録を保持することを検証しました。切替は以降のタスクの経路選択に適用し、進行中のChatを強制終了しません。
- GitHubを取得元とする別の一時ホームで、試験ブランチの0.3.0からnative marketplace upgradeを実行し、登録元のmainへ戻ることを確認しました。

通常Chatの送受信やモデル試験はこの統合確認では追加していません。通常会話での有効性・品質・節約率の実証とは区別します。プラグイン更新時も端末ごとの `chatgpt.json` を配布ファイルに含めず、他端末の既定値はオンにしません。

## 0.3.1: 通常作業の観測記録

Windows / Python 3.13で全60テストと配布パッケージ・manifest・skill検証を通過しました。観測は一時Codexホームで確認しています。

- 通常作業3件の予約、同taskの再送、設定確認の除外、方針内容ごとの保存先分離。版だけが変わった場合は同じ観測群を継続します。
- 記録完了・欠測・部分的な使用量の区別。未完了run、途中turn、未知カウンターを実測充足として扱わず、キャッシュと推論の内数関係を検証します。
- 再開時の既存run・取得済み値・検証参照の保持。不正入力・破損・書込競合で既存記録を上書きせず、方針フックの配信は続けます。
- プロセスを一時ホーム内で異常終了させても、OSロックが解放され後続の記録を続けられること。
- 保存済みのキーで旧観測群を更新でき、別の群が破損・満杯でも走査・変更しないこと。
- 公開用の親/子入力例を実際のCLIプロセスから受理し、3枠使用後はフックが新規開始コマンドを追加しないこと。

実際のCodex CLI `0.154.0-alpha.6.2`を使った一時ホームへの導入・再導入・解除も成功し、導入コピーの両フックが所定の形式を返すことを確認しました。利用中の設定・フック信頼・プラグインは変更していません。以前の自動更新・Windowsタスク登録機能に変更はありません。

通常会話でAIが記録を最後まで更新する実績と、品質や契約使用量の削減は未確認です。台帳は渡された構造化データの整合を検証するもので、実行モデル・使用量の自動取得や、提出された根拠自体の真偽の検証は行いません。親の最終turnが未完了なら欠測を残し、全体の節約率を確定しません。

## 0.3.0試験版: 通常Chatへの任意ルーティング

2026-09-14、Windows / Python 3.13のローカル検証です。

- 単体テスト63件で、既定経路の保持、有効・無効・不正設定、親へのみ追加するフック、依頼ID・材料hashの対応、改変bundleと別依頼の返答の拒否を確認しました。材料中の命令を指示として扱わないプロトコルを採用しています。
- 一時Codexホームで実際のCLIから導入・更新・解除し、導入されたコピーのSessionStartだけに通常Chat経路が追加されることを確認しました。個別設定・通常のモデル設定・既存の方針上書きは保持されています。
- package、manifest、skillの検証を実行しました。モデルAPI、認証情報、会話履歴の直接読取り、ユーザーフック信頼の変更を行うコードは追加していません。
- 通常ChatのUIで `6 Pro` を選んだ状態と、直接送受信・GitHub/公式Docs MCP読み取りは、実機の接続検証で確認しています。バックエンドのモデルIDはツールで取得できないため未確認です。

この版はインライン本文の受け渡しです。独自MCPの公開・書き込み、自動ファイル添付、別ホストでの実行、無人スケジュール実行は対象外です。CLI照合は応答の形式と対応関係のみを確認し、品質、モデル、利用枠の保証はしません。

```text
python -m unittest discover -s tests -v
python scripts/check_package.py
python scripts/smoke_native.py --output-dir outputs/chatgpt-native
```

## 0.2.0: 通常の起動方法を保つバックグラウンド更新

2026-09-14、Windows / Python 3.13 / Codex CLI `0.154.0-alpha.6.2`で検証しました。

- 単体テスト50件で、稼働中・検知不能時の見送り、更新直前の再確認、異常終了で解放されるOSロック、親CLI終了後に残った子プロセスの停止、認証と接続エラーの分類、設定保持を確認しました。
- 実際のCodex CLIとローカルGit fixtureを一時Codexホームで使用し、mainの版更新、固定commitの維持、取得先が利用できない場合の導入済み版の保持を確認しました。アプリ起動やモデルへの依頼は行いません。
- Windowsの実タスクスケジューラで、検証用ホーム専用タスクの登録、15分間隔と次回実行予定、安定entryの実行、実際のCodexプロセスを検知した更新見送り、再登録、解除を確認しました。パスの大文字・小文字が異なる指定、Pythonの場所が変わった場合、runtimeメタデータ破損後の解除も検証しました。検証用タスクは解除済みです。
- 継承した`CODEX_HOME`が別の場所でも、明示したホームだけを更新すること、Windowsの`pythonw.exe`からCLIを実行できること、新版の更新処理が標準CLIの取得元から見つかることを確認しました。
- `config.toml`・`AGENTS.md`・`overrides.json`はバイト単位で不変です。実利用環境への登録・プラグイン更新・フック信頼変更は行っていません。
- 再導入では完全なruntimeを作ってから参照を切り替えます。書き込み失敗を注入し、不完全なruntimeを有効にしないことを確認しました。新版runtimeを読み込めない場合は、保存済みの完全なruntimeへ戻ることも検証しました。
- プラグイン配布ファイルの変更時にmanifest versionも変えるCI検査を追加しました。

```text
python scripts/smoke_updater.py --output-dir outputs/updater-native
python scripts/smoke_scheduler.py --output-dir outputs/scheduler-native
```

両方とも未作成の出力先を指定します。前者にはGitとCodex CLIが必要です。HTTPSのfixture URLはプロセス限定のGit設定でローカルリポジトリに向け、ネットワーク・モデル・実アプリを使いません。外側で稼働中のCodexを停止せず更新を検証するため、**一時ホーム内の試験に限り**稼働プロセスの取得結果を差し替えます。実運用のプログラムに稼働チェックを迂回するCLIオプションはありません。

後者はWindows上で、CodexかChatGPTが稼働している状態で実行します。検証用タスクを明示的に実行し、実際のプロセス検知で更新を見送ることを確認します。利用者の通常タスクや起動ショートカットを変更しません。

15分間隔の自然発火、Codex終了後の本番GitHubからの更新、他OSのCodexホストでの実更新、native upgrade自体が強制終了した場合の原子性は未検証です。取得失敗時の既存版の保持と、更新途中のあらゆる障害からの復元保証は区別します。通常起動と更新開始の完全な排他はできません。起動方法はそのままですが、毎回の起動直前に最新版へ更新される保証はありません。

## 0.1.0: 初版

検証日: 2026-09-14。対象: `codex/initial-plugin` の0.1.0実装。

## 確認した範囲

- 既定の詳細方針・分類表・引継ぎ例が、配布向けに調整した正本スナップショットと一致すること。
- モデル・effort・分担原則の部分上書き、未知キー・型違い・範囲外・重複キー・未解決参照の拒否。
- 生成物のhash、出力先の保護、開始・再開・子の開始イベント用のコンテキスト、旧指示との競合検出。レビューで追加したoverride優先順位とキャッシュの読み取り専用診断も対象に含めます。
- 公式plugin-creatorのmanifest検証、skill-creatorのスキル検証、同梱ファイルの構成と公開前検査。
- Windows上のCodex CLI `0.154.0-alpha.6.2` で、一時的なCodexホームへの登録・導入・版更新・解除。既存設定・指示・上書きファイルを保持し、導入されたコピーから診断と両イベントのフック出力を確認。

通常の利用環境の設定・指示は変更していません。CLI検証はモデルへのリクエストを送らず、フックの信頼設定も変更しません。

## 再実行

Python 3.11以上で、リポジトリ直下から実行します。

```text
python -m unittest discover -s tests -v
python scripts/check_package.py
python scripts/smoke_native.py --output-dir outputs/native-check
```

最後のコマンドにはCodex CLIが必要です。PATH外の実行ファイルは `--codex` で指定できます。出力先は未作成のディレクトリを指定してください。検証用ホームと `result.json` を残し、ユーザーのCodexホームへ書き込みません。初版から次版へのローカル更新を模擬します。

GitHub ActionsではWindows・Linux・macOSのPython 3.11で単体テストとパッケージ検査を実行します。対象commitの実際の結果はPRのChecksを参照してください。Codexホストの動作まで3 OSで検証するものではありません。

## 未検証・制約

- ユーザーがフックを信頼した後の、実際の通常タスクへのイベント配信。
- 日常の仕事におけるmodel/effort選定と、下位モデルからの適切な返却。合格した単体テストだけでは品質や節約を証明できません。
- 別PC・他OSのCodexホスト。モデルとeffortの利用可否はホストごとに確認が必要です。
- 自然言語の上書きによる意味の矛盾。要約と関連する詳細節を合わせてレビューしてください。

競合検出は既知の分担見出し・正本名を探す限定的な診断です。各ディレクトリでは非空の `AGENTS.override.md` を優先します。独自のfallbackファイル名・プロジェクトルート設定・表現の異なる同種の指示を網羅するものではありません。symlink・Windowsのreparse pointを含む入出力パスは保護のため拒否します。

## 正本からの調整

元のモデル、effortの既定値、分担条件は保持しています。個人パス、導入日の履歴、従来の共通指示からの参照経路を配布形式へ変更しました。個人用の内部ログ解析スクリプトは同梱せず、実行情報を取得できない場合は欠測として扱います。effortコードと重複する日本語表示名は、上書き後に古い表示が残らないよう整理しました。元ファイルのhashと調整内容は `defaults/provenance.json` にあります。
